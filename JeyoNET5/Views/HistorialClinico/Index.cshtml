@model IEnumerable<JeyoNET5.Models.HistorialClinico>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Paciente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Detallles)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Paciente.PacienteId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Detallles)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HistorialClinicoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HistorialClinicoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HistorialClinicoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
